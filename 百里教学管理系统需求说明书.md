# 百里教学管理系统 — 需求说明书

> 文档用途：供在新环境从零重新开发时，作为**功能需求 + 界面交互效果**的总蓝本。
> 本文只讲"要做什么、页面长什么样、怎么操作交互"，不含具体技术实现细节。
> 品牌信息：**百里口才｜百里演说**，口号"让演说成为孩子自信生长的力量！"

---

## 目录
- [0. 文档说明](#0-文档说明)
- [1. 系统概述](#1-系统概述)
- [2. 整体架构与信息架构](#2-整体架构与信息架构)
- [3. 用户角色与权限](#3-用户角色与权限)
- [4. 全局 UI/UX 设计规范](#4-全局-uiux-设计规范)
- [5. 学员端需求详解](#5-学员端需求详解)
- [6. 教师端需求详解](#6-教师端需求详解)
- [7. 后台管理系统需求详解](#7-后台管理系统需求详解)
- [8. 共享/通用组件库](#8-共享通用组件库)
- [9. 核心业务规则与状态机](#9-核心业务规则与状态机)
- [10. 多角色切换与登录流程](#10-多角色切换与登录流程)
- [11. 关键交互效果清单](#11-关键交互效果清单)
- [12. 重新开发的里程碑建议](#12-重新开发的里程碑建议)

---

## 0. 文档说明

| 项 | 内容 |
|----|------|
| 目标 | 让新开发环境仅凭本文 + 设计规范即可重建一致的产品 |
| 覆盖范围 | 学员端（微信小程序/移动端）、教师端（移动端）、后台管理系统（PC Web） |
| 本文侧重 | 功能清单、页面结构、信息展示形态、操作交互、状态流转、视觉规范 |
| 不涉及 | 具体代码、框架选型、接口字段、部署细节（由重新开发时自行决策） |

---

## 1. 系统概述

### 1.1 项目背景
作为教育品牌创始人，需要一个系统来管理学员的**作业练习、打卡、积分激励**，并通过**朋友圈分享营销**扩大品牌影响力，同时提供**后台管理系统**支撑日常运营。

### 1.2 三大核心目标
1. 为学员提供便捷的作业练习与打卡工具，形成学习习惯。
2. 建立完整的积分激励体系，激励学习 + 驱动分享传播。
3. 提供教师端 + 后台管理，支撑教学与运营管理高效运转。

### 1.3 核心业务主链路
```
管理员/教师发布任务(作业或打卡) 
   → 学员查看任务、提交作业/打卡、分享得积分
   → 教师批改、积分核算、排行激励
   → 后台管理系统维护学员/教师/课程/班级/排课/任务
   → 循环激励促进学习与传播
```

---

## 2. 整体架构与信息架构

### 2.1 三端产品形态
| 端 | 载体 | 用户 | 核心使命 |
|----|------|------|----------|
| 学员端 | 微信小程序 / 移动端 | 学员（家长） | 完成作业、打卡、赚积分、看排行、分享 |
| 教师端 | 移动端（同款 App） | 教师 | 发布/批改任务、排课、管理积分 |
| 后台管理 | PC Web | 管理员 | 数据总览、人员/课程/班级/排课全量管理 |

### 2.2 信息架构总览
```
百里教学管理系统
├── 学员端
│   ├── 登录（手机号 + 验证码 / 微信登录）
│   ├── 首页（我的概览）
│   ├── 任务中心（作业/打卡列表、详情、提交、记录）
│   ├── 积分中心（当前积分、规则、明细、排行榜）
│   ├── 课程中心（课程列表、课程详情）
│   ├── 班级中心（班级列表、班级详情）
│   └── 个人中心（资料、多账号切换、角色切换、设置）
├── 教师端
│   ├── 教学概览首页
│   ├── 任务管理（列表、发布新任务、批改任务）
│   ├── 排课管理（周课表）
│   ├── 积分管理（记录、规则、调整）
│   ├── 课程/班级查看（只读）
│   └── 个人中心（角色切换、退出）
└── 后台管理系统
    ├── 登录
    ├── 数据仪表盘
    ├── 学员管理（增删改查、批量导入）
    ├── 教师管理（增删改查）
    ├── 课程管理（增删改查）
    ├── 班级管理（增删改查）
    └── 排课管理（排课 + 课表）
```

---

## 3. 用户角色与权限

| 角色 | 职责 | 可见范围 |
|------|------|----------|
| 学员（家长） | 用家长手机号注册，完成作业/打卡、获取积分、分享 | 自己的任务、积分、排行、所属课程/班级 |
| 教师 | 布置任务、批改任务、管理积分、查看排课 | 自己班级相关任务与学员 |
| 管理员 | 系统管理、权限控制、数据统计 | 全校数据、全量人员/课程/班级/排课 |

---

## 4. 全局 UI/UX 设计规范

> 重新开发时建议统一维护一套 **设计令牌（token）**，三端共用，保证一致。下为主观规范清单。

### 4.1 品牌色
| 用途 | 色值 |
|------|------|
| 主色（品牌橙） | `#FF6B35` |
| 主色渐变（历史版本） | `#667eea → #764ba2`（紫渐变，用于旧版首页） |
| 打卡色（火热橙红） | `#ee5a24` |
| 成功 / 警告 / 危险 / 信息 | `#52c41a` / `#faad14` / `#ff4d4f` / `#1890ff` |
| 强调紫 | `#722ed1`、`#a855f7` |

> 交互提示：**同一功能三端主色必须一致**（二次开发时以品牌橙 `#FF6B35` 为准，或沿用紫色渐变保持辨识度，二选一并全局统一）。

### 4.2 字体规则
| 用途 | 字号 | 字重 |
|------|------|------|
| 大标题 | 18px+ | 700 |
| 页面/卡片标题 | 16px | 600 |
| 正文 | 14px | 500 |
| 辅助文字 | 12-13px | 400 |
| 次要/占位文字 | 12px | 400，颜色 `#999` |

### 4.3 间距、圆角、阴影
- 页面边距 16px；卡片内边距 16px；卡片间距 12px；元素间距 8px。
- 卡片圆角 12px；按钮 8px；标签 12px；输入框 8px。
- 默认阴影 `0 2px 8px rgba(0,0,0,.08)`；悬浮 `0 4px 16px rgba(0,0,0,.12)`。

### 4.4 状态标签配色（高频，务必统一）
| 标签 | 背景色 | 文字色 |
|------|--------|--------|
| 班级 | `#e6fffb` | `#08979c` |
| 单次作业 | `#e6f7ff` | `#1890ff` |
| 周期作业 | `#f9f0ff` | `#722ed1` |
| 打卡作业 | 渐变红 | 白色 |
| 已完成 | `#f6ffed` | `#52c41a` |
| 待完成/进行中 | `#fffbe6` / 主色浅底 | `#faad14` / 主色 |
| 已逾期 | `#fff1f0` | `#ff4d4f` |
| 进行中/正常 | 主色浅底（`#FF6B35` 的 10% 透明度） | 主色 |

---

## 5. 学员端需求详解

### 5.1 登录
- **登录方式**：手机号 + 短信验证码（带倒计时按钮）；微信登录（预留）；测试账号快捷登录（测试环境）。
- **首次流程**：输入手机号、姓名 → 勾选《用户协议》《隐私政策》→ 登录 → 进入首页。
- **界面元素**：品牌 Logo、App 名称与 slogan、手机号输入（前缀 +86）、验证码输入 + "获取验证码/倒计时",主登录按钮（主色渐变）,协议勾选框。
- **交互效果**：输足条件才高亮可点登录；获取验证码后按钮进入 60s 倒计时并置灰；登录成功后平滑跳转首页。

### 5.2 首页（我的概览）
- **顶部**：品牌标题"百里口才｜百里演说"+ 副标语。
- **用户卡片**：头像、姓名、⭐当前积分、🔥连续打卡天数。
- **数据统计格（4 宫格）**：待完成作业 / 已完成作业 / 连续打卡 / 累计打卡（各带 emoji 图标）。
- **快捷入口（横向可滑动）**：去打卡、作业列表、积分中心、我的课程 等。
- **今日待办列表**：展示今日需完成的任务，每项含类型图标（📝作业/🔥打卡）、标题、描述（如"待完成·截止明天"），点击"去完成 ›"跳转详情。
- **空态**：今日无待办时显示"今日无待办"。

### 5.3 任务中心（核心模块）

#### 5.3.1 任务列表页
- **顶部统计卡**：待完成数量 / 连续打卡天数。
- **双行筛选 Tab**（横向滚动）：
  - 类型：全部 / 作业 / 打卡
  - 状态：全部 / 进行中(待完成) / 已完成
- **任务卡片**（复用组件）：
  - 头部分区：类型标签（作业/打卡）、频次标签（每日一次/每日多次/每周固定）、状态徽标（进行中/已完成/已结束）。
  - 主体：任务标题、班级名、日期区间。
  - 进度条：`进度 x/y` + 完成百分比 + 可获得积分（`+N积分`），进度条渐变填充、带宽度过渡动画。
  - 底部操作按钮（按状态切换）：
    - 进行中：主按钮"立即完成/立即打卡"+（打卡类附加）"分享"
    - 已完成：次按钮"查看详情"+（作业类附加）"分享"
    - 今日已完成：禁用按钮"今日已完成"
    - 已结束：禁用按钮"已结束"
- **交互效果**：点击卡片跳详情；点击操作按钮触发对应动作；空态显示"暂无任务"。

#### 5.3.2 任务详情页
- 顶部分区：类型徽标 + 频次徽标 + 任务标题 + 班级/日期 meta 行。
- 进度区块：`进度 x/y` + 渐变进度条。
- **积分规则卡（📖）**：逐条列出"完成任务 +N积分""每日打卡 +N积分""分享朋友圈 +N积分"等。
- **今日任务卡**：右上角状态"已完成/未完成"。
- **提交记录区**：列出最近提交项，标题栏右侧"查看全部 >"跳转记录页。
- 底部：主操作按钮（立即完成/立即打卡）。

#### 5.3.3 任务提交页
- 任务信息卡：标题、类型、`今日日期` + `第 n 天`。
- **上传作品区**："点击上传图片/视频/音频"，已选文件展示缩略图/媒体图标 + 文件名，可单个删除（✕）。
- **文字说明区**：textarea（选填，最大 500 字），右下角实时字数 `x/500`。
- 底部：加载态"提交中..."，禁用逻辑（今日已提交则不可再提）。

#### 5.3.4 提交记录页
- **顶部任务汇总卡**：已提交天数 / 总天数。
- **记录列表卡**：每条含日期、`第 n 天`、状态（已完成/待完成）、文件列表、`+N积分`、分享标记（已分享好友/已分享朋友圈）、老师评价（等级、额外积分、评语）。

### 5.4 打卡模块
- **打卡概览指标**：累计打卡、连续打卡、最长连续、今日打卡。
- **入口**：首页快捷操作"去打卡"。
- **打卡作业列表**：仅显示带打卡属性的任务，卡片样式同任务卡。
- **打卡交互**：
  - "立即打卡"：自动定位到最早未完成周期/次数 → 自动展开 → 高亮目标项 → 滚动到可视区。
  - "补打卡"：仅截止前可用；超出则提示"作业已截止，无法补打卡！"。
- **打卡日历**（班级/个人页展示）：月视图，可上/下月切换；今天高亮；已打卡日期显示 ✓；底部统计（累计/连续/本月打卡）。

### 5.5 分享功能（营销核心）
- **分享面板（底部弹出 Sheet）**：
  - 预览卡片：标题（自动生成如"🔥 我在百里口才完成了《xxx》，已完成 x/y！"）+ 副标语 + 徽章占位图。
  - 两个分享选项：微信好友 💬（绿色圆钮，不限次）、朋友圈 ⭕（绿色圆钮，标注 `+N积分`，每周期限一次）。
  - 底部"取消"按钮。
- **前置校验**：普通作业须"完成全部 X 次作业后才能分享"，否则提示；已全部完成提示"作业全部完成，可以分享！"。
- **分享成功弹窗（🎉）**：展示"已分享到微信/朋友圈 + 快去邀请朋友围观 + +N积分 + [知道了] 按钮"。

### 5.6 积分中心
- **积分大卡**：当前积分（大字）+ 累计获得 / 累计扣除。
- **积分规则卡（📖）**：列出规则条目（打卡+10、完成作业+20、连续打卡+5~50、分享+5 等）。
- **快捷操作**：排行榜 🏆 / 积分记录 📜。
- **本周积分获取**：周一到周日逐日展示当日得分，有积分的日子标 ✓ 并高亮。

### 5.7 积分排行榜
- **双 Tab**：
  - 时间周期：周榜 / 学期榜(月) / 年度榜
  - 范围：班级 / 校区
- **前三名区**：居中"冠军"高亮度 + 两侧"亚军/季军"，奖杯 🥇🥈🥉，金色/银色/铜色背景，显示头像、姓名、分数。
- **我的排名条**：🏅 排名 + 分数。
- **榜单列表**：逐行显示名次、头像、姓名、分数；"我"高亮（橙色徽标）；**非本人姓名中间字隐藏（*）**。
- **空态**："暂无排行榜数据"。

### 5.8 课程中心
- **课程列表页**：筛选 Tab（全部/报名中/进行中/已结课）；课程卡片含封面、名称、级别、课时、班级信息、状态标签。
- **课程详情页**：封面大图 + 名称 + 级别 + 课时 meta；"课程介绍"区块；"班级列表"区块（班级卡片，点击进班级详情）。
- **班级卡片**：名称、主讲老师、人数、上课时间、状态。

### 5.9 班级中心
- **班级列表页**：筛选 Tab（全部/线上班/线下班）；班级卡片。
- **班级详情页**：封面图；信息卡（主讲老师、上课时间、上课地点、班级人数）；Tab 切换（课程表 / 作业 / 打卡 / 成员 / 通知）；课程表用打卡日历；成员列表可标"班长"；通知列表。

### 5.10 个人中心（我的）
- **头部**：头像、姓名、当前积分。
- **多账号切换**：账户列表（当前账户标"当前账户"，其他如"妹妹"），点击切换，可"+ 添加账号"。
- **菜单导航**：我的课程、排行榜、编辑资料、设置、联系我们等。
- **角色切换**：入口进入角色切换页。
- **退出登录**：弹确认框（"确定要退出登录吗？"）→ 回到登录页。

---

## 6. 教师端需求详解

### 6.1 教学概览首页
- **顶部**：教师头像/姓名（首字圆形头像）+ "教师"角色标签，主色渐变背景。
- **统计卡**：学员总数 / 待批改 / 今日课程（3 项 + 分隔线）。
- **快捷操作**：发布任务 📝 / 批改任务 ✅ / 排课管理 📅 / 积分管理 ⭐。
- **待批改任务区**：任务标题、班级、`已提交数/总数`、"待批改"警示标签；右侧"查看全部"。

### 6.2 任务管理（列表）
- Tab：全部 / 作业 / 打卡。
- 复用任务卡片（同学员端样式）。
- **右下角悬浮按钮（FAB "+"）**：点击进入发布任务。

### 6.3 发布任务（核心表单）
> 这是教师端最复杂的表单，重新开发时务必保留以下分组与字段。

**任务基础**：目标班级、任务标题、任务类型（作业/打卡）。
**时间与频次**：
- 开始日期、结束日期。
- 频次模式：每日一次 / 每日多次 / 每周固定（选周固定时展示周一~周日多选）。
**打卡专属设置**：允许跳过打卡、允许补打卡、补卡次数上限（打卡任务时才显示）。
**积分设置**：提交 +N、分享朋友圈 +N、分享好友 +N、完成奖励 +N、达标阈值。
**分享设置**：开启分享、允许分享朋友圈、允许分享好友。
**附件要求**：是否必传、可传类型多选（视频/音频/图片）、最大数量、大小上限。
**状态操作**：保存草稿（状态=草稿）/ 发布（状态=进行中）。
- **校验**：依次校验班级、标题、起止日期、星期选择，未通过用轻提示（toast）阻止。
- **交互**：提交成功后提示"发布成功/保存成功"，短暂延迟后返回上一页。

### 6.4 批改任务
- 任务卡列表（待批改列表）：标题 + "待批改"标签 + 学员、提交时间、班级信息。
- 操作：查看详情 / 批改。
- **批改弹窗**：弹出批改框，可输入评语，可设置等级（A/B/C/D）、额外积分，确认后提示"批改成功"。

### 6.5 排课管理
- Tab：本周 / 下周 / 本月。
- **周课表格**：纵向时间轴（多时段）+ 横向星期列；单元格显示课程名、班级、教师、教室、人数。
- 点击单元格查看详情；右上角"+"新增排课（跳新增流程）。
- 交互：横/纵向可滚动；当前日期高亮。

### 6.6 积分管理
- Tab：积分记录 / 积分规则。
- 积分记录：逐条类型（获得/消耗）、来源（作业/打卡/分享/手动）、变动量、时间、描述。
- 积分规则：规则列表。
- 右下角 FAB "+"：调整积分（手动增减）。

### 6.7 课程/班级（只读）
- 供教师查看自己相关的课程与班级信息（复用学员端同款卡片与详情）。

### 6.8 个人中心
- 含角色切换入口、菜单导航、退出登录（同学员端结构）。

---

## 7. 后台管理系统需求详解

> PC Web 管理端，基于通用后台 UI 框架（如 Element Plus 风格）搭建。

### 7.1 登录
- 独立后台登录页，账号密码登录；登录态校验，未登录跳登录页，已登录访问登录页自动回首页。

### 7.2 布局（Layout）
- 左侧菜单导航 + 顶部/主体内容区 + 退出登录，典型后台布局。

### 7.3 数据仪表盘
- **4 个统计大卡**：学员总数 / 教师总数 / 课程总数 / 班级总数（各配彩色渐变图标）。
- **近期任务表**：任务名称、类型（作业/打卡标签）、班级、状态（进行中/草稿标签）、创建时间。
- **快捷操作区**：新增课程、新增班级、排课管理 按钮。

### 7.4 学员管理
- **学员列表表格**：ID、姓名、手机号、班级、积分（可排序）、状态（正常/禁用标签）、操作（编辑/删除）。
- 顶部"新增学员"按钮；分页（页码/每页条数/总数/跳页）。
- **新增/编辑**：弹窗或抽屉表单。
- **删除**：确认弹框（"确定要删除该学员吗？"），确认后提示"删除成功"。
- **批量导入**（规划）：支持批量导入学员。

### 7.5 教师管理
- 教师列表表格 + 新增/编辑/删除（交互同学员管理）。

### 7.6 课程管理
- 课程列表表格 + 新增/编辑/删除（交互同前）。

### 7.7 班级管理
- 班级列表表格 + 新增/编辑/删除（关联课程、班主任、学员）。

### 7.8 排课管理
- 班级排课、教师课表、周视图课表（同教师端排课，但为全量管理视角）。

---

## 8. 共享/通用组件库

> 重新开发时建议抽象为通用组件，三端复用，保证体验一致。

| 组件 | 用途/交互 |
|------|-----------|
| Button 按钮 | 主/次/禁用态，loading 态（文字变化如"提交中..."），尺寸 small/large |
| Card 卡片 | 统一圆角/阴影容器 |
| Navbar 顶部导航 | 自定义标题、返回 |
| Tabs 选项卡 | 顶部/分段切换，选中态高亮（主色文字/指示器/背景） |
| Toast 轻提示 | 操作反馈（成功、失败、信息） |
| Modal 弹窗 | 确认框、批改框、分享成功弹窗等 |
| Loading 加载 | 列表加载占位 |
| EmptyState 空态 | 各列表无数据处理（图标 + 文案） |
| TaskCard 任务卡片 | 作业/打卡统一卡片（类型/频次/状态/进度/操作） |
| SubmitRecord 提交记录 | 单条提交记录展示 |
| SharePanel 分享面板 | 底部弹出分享 Sheet |
| PointsRecord 积分记录 | 单条积分明细 |
| PointsRule 积分规则 | 规则条目列表 |
| CheckinCalendar 打卡日历 | 月视图打卡展示 |
| RankingItem 排行榜行 | 行名次/头像/姓名/分数/我的高亮 |
| CourseCard/ClassCard | 课程/班级卡片 |
| SharePanel | 底部分享面板 |

---

## 9. 核心业务规则与状态机

### 9.1 任务状态
任务对学员而言的状态流转：
```
未开始(未到开始日期) → 进行中/待完成 → 已完成(进度满)  或 → 已结束(超过截止)
```
对应卡片上的三态标签：**进行中 / 已完成 / 已结束**。

### 9.2 作业类型与频次
- 类型：作业（homework）/ 打卡（checkin）。
- 频次：每日一次 / 每日多次 / 每周固定。
- 周期概念：多周期任务按天数分周期，每周期要求完成多次；进度 = 已完成天数 / 总天数。
- 提交：多媒体附件（图片/音频/视频）可多选 + 文字说明（选填，限 500 字）。

### 9.3 分享规则
- 微信好友：不限次数（无积分）。
- 朋友圈：视任务配置给 `+N积分`，**每周期限一次**。
- 普通作业须先全部完成才能分享；打卡作业按周期分享。

### 9.4 积分规则
| 场景 | 积分 |
|------|------|
| 完成作业/打卡 | 教师/任务配置（提交、完成奖励） |
| 分享朋友圈 | 任务配置 |
| 连续打卡 | 按天数阶梯 |
| 优秀作业加分 | 教师手动（+5~20） |
| 手动调整 | 后台/教师 +- |

### 9.5 排行榜规则
- 维度：周/学期/年度 × 班级/校区。
- 前三名特殊样式；本人高亮；姓名隐私处理（中间字 *）。

---

## 10. 多角色切换与登录流程

- **角色切换页**：展示"学员端 👶 / 教师端 👩‍🏫"两个角色卡，当前角色打勾，点击即切换并进入对应端首页。
- 登录后默认进入对应角色首页；个人中心提供角色切换入口。
- 多账号（如家长绑定多个孩子）可快速切换账户。

---

## 11. 关键交互效果清单（二次开发验收要点）

- 主色/渐变/字体/圆角/阴影在**三端一致**。
- 任务卡片进度条带**宽度过渡动画**。
- 登录验证码**倒计时置灰**。
- 打卡"立即打卡"**自动定位+展开+高亮+滚动**目标项。
- 补卡超截止日期**明确拦截提示**。
- 分享面板**底部弹出**，含直播预览 + 双分享项 + 积分标注；分享成功有**奖励积分弹窗**。
- 提交页字数**实时统计**、附件**可预览可删除**、提交中**loading 反馈**。
- 积分周视图、排行榜我的高亮、前三名奖杯样式。
- 各列表统一**空态**展示。
- 后台管理统一**表格 + 分页 + 确认删除 + 操作反馈**。
- 教师发布任务表单**分组清晰 + 校验提示 + 保存草稿/发布**两态。

---

## 12. 重新开发的里程碑建议

| 阶段 | 内容 |
|------|------|
| M1 设计基础 | 定设计令牌（色/字/距/圆角）、统一组件库、信息架构 |
| M2 学员端核心 | 登录、首页、任务中心（列表/详情/提交/记录）、打卡 |
| M3 学员端激励 | 积分中心、排行榜、分享营销、课程/班级 |
| M4 教师端 | 概览、任务管理/发布/批改、排课、积分 |
| M5 后台管理 | 登录、仪表盘、学员/教师/课程/班级 CRUD、排课 |
| M6 联调收尾 | 三端数据打通、异常/空态完善、验收 |

---

*本文档基于既有项目成果整理，作为从零重新开发的需求与交互蓝本。*